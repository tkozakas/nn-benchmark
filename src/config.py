train_config = {
    'val_split': 0.2,
}

# List (or mapping) of supported off-the-shelf torchvision architectures.
# Extend this mapping if you want to add more models.
supported_architectures = {
    'GoogleNet': {},
    'ResNet18': {},
    'ResNet50': {},
    'DenseNet121': {},
}
